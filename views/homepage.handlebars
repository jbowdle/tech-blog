{{#each posts as |post|}}
<section class ="card">
  <a href="/post/{{id}}">
    <h4>{{title}}</h4>
    <div class="card-body">
      <p>{{content}}</p>
      <p>Posted by: {{user.username}}</p>
      <p>{{createdAt}}</p>
    </div>
  </a>
</section>
{{/each}}